services:
  adguard-home-sync:
    image: ghcr.io/bakito/adguardhome-sync:latest
    container_name: adguard-home-sync
    restart: unless-stopped

    volumes:
      - ./config.yaml:/config/adguardhome-sync.yaml:ro

    command: ["run", "--config", "/config/adguardhome-sync.yaml"]
