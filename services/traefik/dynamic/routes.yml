# This file is generated.
# Do not edit manually.
# Source: config/services.yaml, config/hosts.yaml
# Generated by: scripts/generation/generate-traefik.sh
#
# config/services.yaml declares the https://<name>.home.arpa URLs; this
# file is what actually makes them resolve to something once AdGuard Home
# points *.home.arpa at Traefik (see README.md). A service opts in with a
# `traefik: {enabled: true, port: <n>}` block.
http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.home.arpa`)"
      entryPoints: ["websecure"]
      service: api@internal
      tls: {}

  services:

