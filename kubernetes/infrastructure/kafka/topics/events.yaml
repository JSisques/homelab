apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: events
  namespace: kafka
  labels:
    strimzi.io/cluster: homelab
spec:
  partitions: 3
  replicas: 3

  config:
    retention.ms: 604800000
    cleanup.policy: delete
